<%= form_with(model: peer_teacher, html: { class: "course-form" }) do |form| %>
  <% if peer_teacher.errors.any? %>
    <div class="error-box">
      <h2><%= pluralize(peer_teacher.errors.count, "error") %> prohibited this peer teacher from being saved:</h2>
      <ul>
        <% peer_teacher.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :first_name, class: "form-label" %>
    <%= form.text_field :first_name, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :last_name, class: "form-label" %>
    <%= form.text_field :last_name, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :uin, class: "form-label" %>
    <%= form.text_field :uin, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :email, class: "form-label" %>
    <%= form.text_field :email, class: "form-control" %>
  </div>

  <div class="form-actions">
    <%= form.submit "Save Peer Teacher", class: "btn btn-maroon" %>
  </div>
<% end %>
